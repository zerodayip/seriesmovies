name: M3U MAKER

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */5 * * *'  # Her 5 saatte bir

jobs:
  part1:
    name: Step 1-2-3 Çalıştır
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      
    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-cache-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            pip-cache-${{ runner.os }}-

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml playwright aiohttp aiofiles dotenv zstandard cloudscraper
          playwright install

      # 1. Vavoo IPTV Scripts
      - name: Run KENDI PROXY UZERINDEN YAYIN ALINIYOR.py
        run: python private_py/vavoo/iptv/vavooiptvkendi.py

      # 1. Vavoo IPTV Scripts
#      - name: Run DIGER PROXY UZERINDEN YAYIN ALINMAKTADIR.py
#        run: python private_py/vavoo/iptv/vavooiptvgoldvod.py

      # 1. Gunluk Spor Akısı
      - name: Run GunlukSporAkısı.py
        run: python private_py/channels/gunluksporakısı/tvyayinakisi.py

      # 2. DiziFun Diziler Yerli Scripts
#      - name: Run dizifunyenibolumler.py
#        run: python private_py/series/dizifun/yenibolumler.py

      # 2. DiziLife Diziler Yerli Scripts
      - name: Run dizilifeyenibolumler.py
        run: python private_py/series/dizilife/yenibolumler.py

      # 2. FilmHane Diziler Yerli Scripts
#      - name: Run filmhaneyenibolumler.py
#        run: python private_py/series/filmhane/yenibolumler.py

      # 2. Diziyiİzle Diziler Yerli Scripts
      - name: Run diziyiizleyenibolumler.py
        run: python private_py/series/diziyiizle/yenibolumler.py

      - name: Sync Vavoo outputs to private m3u8file repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git privaate_m3u8file
          cp v/vavoo.m3u privaate_m3u8file/vavoo.m3u || echo "v/vavoo.m3u bulunamadı"
          cp dizifundizi.m3u privaate_m3u8file/dizifundizi.m3u || echo "dizifundizi.m3u bulunamadı"
          cp dizilifedizi.m3u privaate_m3u8file/dizilifedizi.m3u || echo "dizilifedizi.m3u bulunamadı"
          cp filmhanedizi.m3u privaate_m3u8file/filmhanedizi.m3u || echo "filmhanedizi.m3u bulunamadı"
          cp gunluk_spor.m3u privaate_m3u8file/gunluk_spor.m3u || echo "gunluk_spor.m3u bulunamadı"
          cp diziyiizledizi.m3u privaate_m3u8file/diziyiizledizi.m3u || echo "diziyiizledizi.m3u bulunamadı"
          cd privaate_m3u8file
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add vavoo.m3u dizifundizi.m3u dizilifedizi.m3u gunluk_spor.m3u filmhanedizi.m3u diziyiizledizi.m3u
          git commit -m "Canlı Kanal ve Otomatik Embed linklerin çıktısı güncellendi [auto]" || echo "No changes"
          git push origin main

      # 3. Run All JSON
      - name: Run Dizigom JSON Script
        run: python private_py/series/dizigom/dizigomdizijson.py

      - name: Run Sezonlukdizi JSON Script
        run: python private_py/series/sezonlukdizi/tumdizilerjson.py

      - name: Run Anizm JSON Script
        run: python private_py/anime/anizm/anizimjson.py

      - name: Run AnimeNoSub JSON Script
        run: python private_py/anime/ingilizce/animenosub/animenosubjson.py

      - name: Run SetfilmİzleDizi JSON Script
        run: python private_py/series/setfilmizledizi/setfilmizledizijson.py

#      - name: Run Dizipal JSON Script
#        run: python private_py/series/dizipal/dizipaljson.py

      - name: Run DiziYo JSON Script
        run: python private_py/series/diziyo/diziyojson.py

      - name: Commit and Push All JSONs to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add dizigom/*.json || echo "No dizigom files"
          git add sdizi/*.json || echo "No sezonlukdizi files"
          git add anizm/*.json || echo "No anizm files"
          git add animenosub/*.json || echo "No animenosub files"
          git add setfilmizledizi/*.json || echo "No setfilmizledizi files"
          git add dizipal/*.json || echo "No dizipal files"
          git add diziyo/*.json || echo "No diziyo files"
          git commit -m "All JSON güncellendi [auto]" || echo "No changes"
          git push origin main

  part2:
    name: Step 4-5-6-7-8-9-10-11 Çalıştır
    runs-on: ubuntu-latest
    needs: part1
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml playwright aiohttp aiofiles dotenv
          playwright install

      # 4. Sezonlukdizi M3U Maker
      - name: Run SezonlukDizi yenibolumler.py
        run: python private_py/series/sezonlukdizi/yenibolumler.py

      - name: Commit and Push SezonlukDizi JSON to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add sdizi/*.json || echo "No JSON files"
          git commit -m "SezonlukDizi JSON güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync SezonlukDizi output to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/sezonlukdizi.git private_sezonlukdizi
          rsync -a sdizi/ private_sezonlukdizi/
          cd private_sezonlukdizi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "SezonlukDizi çıktı güncellendi [auto]" || echo "No changes"
          git push origin main

      # 5. Dizigom M3U Maker
      - name: Run Dizigom yenibolumler.py
        run: python private_py/series/dizigom/yenibolumler.py

      - name: Commit and Push Dizigom JSON to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add dizigom/*.json || echo "No JSON files"
          git commit -m "Dizigom JSON güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync Dizigom output to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/dizigom2.git private_dizigom2
          rsync -a dizigom/ private_dizigom2/
          cd private_dizigom2
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "Dizigom çıktı güncellendi [auto]" || echo "No changes"
          git push origin main

      # 6. Anizm M3U Maker
      - name: Run Anizm yenibolumler.py
        run: python private_py/anime/anizm/yenibolumler.py

      - name: Commit and Push Anizm JSON to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add anizm/*.json || echo "No JSON files"
          git commit -m "Anizm JSON güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync Anizm output to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/anizm.git private_anizm
          rsync -a anizm/ private_anizm/
          cd private_anizm
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "Anizm çıktı güncellendi [auto]" || echo "No changes"
          git push origin main

     #  7. RecTV Script (PASİF)
#      - name: Run RecTV Script
#        run: python private_py/rectv/rectv.py
      
#      - name: Sync RecTV outputs to private m3u8file repo
#        run: |
#         git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git private_m3u8file
#         mkdir -p private_m3u8file/rec
#         cp r/reccanli.m3u private_m3u8file/rec/ || echo "reccanli.m3u kopyalanamadı"
#         cp r/recfilm.m3u private_m3u8file/rec/ || echo "recfilm.m3u kopyalanamadı"
#         cp r/recdizi.m3u private_m3u8file/rec/ || echo "recdizi.m3u kopyalanamadı"
#         cp r/recanime.m3u private_m3u8file/rec/ || echo "recanime.m3u kopyalanamadı"
#         cd private_m3u8file
#         git config user.name "GitHub Actions"
#         git config user.email "actions@github.com"
#         git add rec/*.m3u
#         git commit -m "RecTV çıktıları güncellendi [auto]" || echo "No changes"
#         git push origin main

      # 8. AnimeNoSub M3U Maker
#      - name: Run AnimeNoSub yenibolumler.py
#        run: python private_py/anime/ingilizce/animenosub/yenibolumler.py

      - name: Commit and Push AnimeNoSub JSON to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add animenosub/*.json || echo "No JSON files"
          git commit -m "AnimeNoSub JSON güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync AnimeNoSub output to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/animenosub.git private_animenosub
          rsync -a animenosub/ private_animenosub/
          cd private_animenosub
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "AnimeNoSub çıktı güncellendi [auto]" || echo "No changes"
          git push origin main

      # 9. SetFilmİzleDizi M3U Maker
      - name: Run SetFilmİzleDizi yenibolumler.py
        run: python private_py/series/setfilmizledizi/yenibolumler.py

      - name: Commit and Push SetFilmİzleDizi JSON to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add setfilmizledizi/*.json || echo "No JSON files"
          git commit -m "SetFilmİzleDizi JSON güncellendi [auto]" || echo "No changes"
          git push origin main

      # Direkt kendi gizli reposuna push yapmaktadır.
      - name: Sync setfilmizledizi output to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/setfilmizledizi.git private_setfilmizledizi
          rsync -a setfilmizledizi/ private_setfilmizledizi/
          cd private_setfilmizledizi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "SetFilmİzleDizi çıktı güncellendi [auto]" || echo "No changes"
          git push origin main

      # 10. Dizipal M3U Maker
#      - name: Run Dizipal yenibolumler.py
#        run: python private_py/series/dizipal/yenibolumler.py

      # Direkt m3ufile gizli reposuna push yapmaktadır.
#      - name: Sync Dizipal outputs to private m3u8file repo
#        run: |
#          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git privaate_m3u8filex
#          cp dizipalyerlidizi.m3u privaate_m3u8filex/dizipalyerlidizi.m3u || echo "dizipalyerlidizi.m3u bulunamadı"
#          cd privaate_m3u8filex
#          git config user.name "GitHub Actions"
#          git config user.email "actions@github.com"
#          git add dizipalyerlidizi.m3u
#          git commit -m "Dizipal linklerin çıktısı güncellendi [auto]" || echo "No changes"
#          git push origin main

      # 11. Diziyo M3U Maker
      - name: Run DiziYo yenibolumler.py
        run: python private_py/series/diziyo/yenibolumler.py

      - name: Commit and Push DiziYo JSON to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add diziyo/*.json || echo "No JSON files"
          git commit -m "DiziYo JSON güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync Diziyo output to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/diziyo.git private_diziyo
          rsync -a diziyo/ private_diziyo/
          cd private_diziyo
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "DiziYo çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          
  part3:
    name: Step 12 Çalıştır
    runs-on: ubuntu-latest
    needs: part2
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      # 12. YeniJSON to AnaJSON
      - name: Run yenibolumdenanajsona Script
        run: python private_py/series/yenibolumdenanajsona.py

      - name: Commit and Push Ana JSONs to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add dizigom/*.json sdizi/*.json anizm/*.json animenosub/*.json setfilmizledizi/*.json diziyo/*.json || echo "No files"
          git commit -m "All Ana JSON güncellendi [auto]" || echo "No changes"
          git push origin main

  part4:
    name: Step 12-14-15-16-17 Çalıştır
    runs-on: ubuntu-latest
    needs: part3
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml aiohttp aiofiles dotenv playwright
          playwright install
       
      # 13. Dizizgom Filmler
      - name: Run dizigomfilmler Script
        run: python private_py/movies/dizigom/dizigomfilmler.py

      # 14. SetFilmİzle Filmler
      - name: Run Setfilm Script
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: python private_py/movies/setfilmizle/ilksayfatümm3uolusturma.py

      # 15. HDFilm Yabancı Filmler
      - name: Run HDFilm Yabancı Filmler Script
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: python private_py/movies/hdfilmsite/ilksayfayabancıfilmler.py

      # 16. HDFilm Yerli Filmler
      - name: Run HDFilm Yerli Filmler Script
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: python private_py/movies/hdfilmsite/ilksayfayerlifilmler.py

      # 17. Dizipal Yerli Filmler
#      - name: Run Dizipal Yerli Filmler Script
#        env:
#          GH_TOKEN: ${{ secrets.GH_TOKEN }}
#        run: python private_py/movies/dizipal/yerlifilm.py

      - name: Sync Filmler outputs to private m3u8file repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git privatate_m3u8file
          cp dizigomfilmler.m3u privatate_m3u8file/dizigomfilmler.m3u || echo "dizigomfilmler.m3u bulunamadı"
          cp xtream/setfilmizlefilmler.m3u privatate_m3u8file/setfilmizlefilmler.m3u || echo "setfilmizlefilmler.m3u bulunamadı"
          cp hdfilmsite.m3u privatate_m3u8file/hdfilmsite.m3u || echo "hdfilmsite.m3u bulunamadı"
          cp hdfilmsiteyerli.m3u privatate_m3u8file/hdfilmsiteyerli.m3u || echo "hdfilmsiteyerli.m3u bulunamadı"
          cp dizipalyerlifilm.m3u privatate_m3u8file/dizipalyerlifilm.m3u || echo "dizipalyerlifilm.m3u bulunamadı"
          cd privatate_m3u8file
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add setfilmizlefilmler.m3u dizigomfilmler.m3u hdfilmsite.m3u hdfilmsiteyerli.m3u dizipalyerlifilm.m3u
          git commit -m "Filmlerin çıktısı güncellendi [auto]" || echo "No changes"
          git push origin main
