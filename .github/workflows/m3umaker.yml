name: M3U MAKER

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'    # Her 6 saatte bir tüm scriptler çalışır

jobs:
  notify_start:
    name: 🔔 Telegram Start Notify
    runs-on: ubuntu-latest

    steps:
      - name: Send Telegram start message
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          MESSAGE="🔔 M3U workflow başladı!"
          
          curl -s -o /dev/null -X POST "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE"
            
  part1:
    name: Step 1-3 Çalıştır
    runs-on: ubuntu-latest
#    needs: notify_start
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-cache-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            pip-cache-${{ runner.os }}-

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml playwright aiohttp aiofiles dotenv zstandard cloudscraper python-dateutil
          playwright install

      # 1. Canlı Yayın ve EPG üretimi
      - name: Run Vavoo Yayınları.py
        run: python private_py/vavoo/iptv/vavooiptvkendi.py

      - name: Run GunlukSporAkısı.py
        run: python private_py/channels/gunluksporakısı/tvyayinakisi.py

      - name: Run epg.py
        run: python private_py/channels/epg/gunlukepg.py

      # 2. TV Dizileri
      - name: Run yereldiziler.py
        run: python private_py/series/yereldiziler/yereldiziler.py

      - name: Commit and Push EPG to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add xtream/tum_kanallar.xml xtream/tum_kanallar.gz || echo "No files"
          git commit -m "Günlük EPG güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync M3U outputs to private repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git privaate_m3u8file

          cp v/vavoo.m3u privaate_m3u8file/vavoo.m3u || echo "v/vavoo.m3u bulunamadı"
          cp gunluk_spor.m3u privaate_m3u8file/gunluk_spor.m3u || echo "gunluk_spor.m3u bulunamadı"
          cp televizyondizileri.m3u privaate_m3u8file/televizyondizileri.m3u || echo "televizyondizileri.m3u bulunamadı"

          cd privaate_m3u8file
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Auto playlist update" || echo "No changes"
          git push origin main

      # 3. Run All JSON
      - name: Run Dizigom JSON Script
        run: python private_py/series/dizigom/dizigomdizijson.py

      - name: Run Sezonlukdizi JSON Script
        run: python private_py/series/sezonlukdizi/tumdizilerjson.py

      - name: Run Anizm JSON Script
        run: python private_py/anime/anizm/anizimjson.py

      - name: Run AnimeNoSub JSON Script
        run: python private_py/anime/ingilizce/animenosub/animenosubjson.py

      - name: Run SetfilmİzleDizi JSON Script
        run: python private_py/series/setfilmizledizi/setfilmizledizijson.py

      - name: Run Dizipall JSON Script
        run: python private_py/series/dizipall/jsonyonlendirmeguncelleme.py

      - name: Run HDFilmCehennemi JSON Script
        run: python private_py/series/hdfilmcehennemi/dizilerjson.py

      - name: Run DiziYo JSON Script
        run: python private_py/series/diziyo/diziyojson.py

      - name: Run YabancıDiziler JSON Script
        run: python private_py/series/yabancidiziler/dizilerjson.py

      - name: Commit and Push All JSONs to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add dizigom/*.json || echo "No dizigom files"
          git add sdizi/*.json || echo "No sezonlukdizi files"
          git add anizm/*.json || echo "No anizm files"
          git add animenosub/*.json || echo "No animenosub files"
          git add setfilmizledizi/*.json || echo "No setfilmizledizi files"
          git add diziyo/*.json || echo "No diziyo files"
          git add yabancidiziler/*.json || echo "No yabancidiziler files"
          git add dizipall/diziler.json || echo "No diziyo files"
          git add hdfilmcehennemi/yenibolumler.json || echo "No hdfilmcehennemi files"
          git commit -m "All JSON güncellendi [auto]" || echo "No changes"
          git push origin main

  part2:
    name: Step 4 Çalıştır
    runs-on: ubuntu-latest
    needs: part1
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      # 4. DİZİLER KATEGORİ GÜNCELLEME
      - name: Run series_kategori_guncelleme Script
        run: python private_py/series/category.py

      - name: Commit and Push Ana JSONs to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add xtream/series_category.json || echo "No files"
          git commit -m "Series Category JSON güncellendi [auto]" || echo "No changes"
          git push origin main
          
  part3:
    name: Step 5-to-15 Çalıştır (Paralel)
    runs-on: ubuntu-latest
    needs: part2
    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml playwright aiohttp aiofiles dotenv httpx
          playwright install

      - name: Run Scripts in Controlled Parallel Batches
        run: |
          set +e
      
          echo "🔄 Batch 1 başlıyor (HDFilmSite & Anizm & AnimeNoSub & SetFilmİzle & Diziyo)"
          ( python private_py/series/hdfilmsite/yenibolumler.py || true ) &
          ( python private_py/anime/anizm/yenibolumler.py || true ) &
          ( python private_py/anime/ingilizce/animenosub/yenibolumler.py || true ) &
          ( python private_py/series/setfilmizledizi/yenibolumler.py || true ) &
          ( python private_py/series/diziyo/yenibolumler.py || true ) &
          wait
      
          echo "🔄 Batch 2 başlıyor (Dizigom & Dizipall & HDFilmCehennemi & YabancıDiziler & Dizipal)"
          ( python private_py/series/dizigom/yenibolumler.py || true ) &
          ( python private_py/series/dizipall/yenibolumler.py || true ) &
          ( python private_py/series/hdfilmcehennemi/yenibolumler.py || true ) &
          ( python private_py/series/yabancidiziler/yenibolumler.py || true ) &
          ( python private_py/series/dizipal/yenibolumler.py || true ) &
          wait
      
          echo "🔄 Batch 3 başlıyor (SezonlukDizi & AnimeHeaven & DiziLife & Tlc & Dmax)"
          ( python private_py/series/sezonlukdizi/yenibolumler.py || true ) &
          ( python private_py/anime/ingilizce/animeheaven/yenibolumler.py || true ) &
          ( python private_py/series/dizilife/yenibolumler.py || true ) &
          ( python private_py/series/tlc/yenibolumler.py || true ) &
          ( python private_py/series/dmax/yenibolumler.py || true ) &
          wait
      
          echo "✅ Tüm scriptler tamamlandı.."

      - name: Commit and Push All JSONs
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git config status.showUntrackedFiles no
          git add **/*.json || echo "No JSON files"
          git commit -m "Tüm JSON dosyaları güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Sync Outputs to Private Repos
        run: |
          # Dizigom
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/dizigom.git private_dizigom
          rsync -a dizigom/ private_dizigom/
          cd private_dizigom
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "Dizigom çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # Anizm
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/anizm.git private_anizm
          rsync -a anizm/ private_anizm/
          cd private_anizm
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "Anizm çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # AnimeNoSub
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/animenosub.git private_animenosub
          rsync -a animenosub/ private_animenosub/
          cd private_animenosub
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "AnimeNoSub çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # SetFilmİzleDizi
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/setfilmizledizi.git private_setfilmizledizi
          rsync -a setfilmizledizi/ private_setfilmizledizi/
          cd private_setfilmizledizi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "SetFilmİzleDizi çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # DiziYo
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/diziyo.git private_diziyo
          rsync -a diziyo/ private_diziyo/
          cd private_diziyo
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "DiziYo çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # DiziPall
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/dizipall.git private_dizipall
          rsync -a dizipall/ private_dizipall/
          cd private_dizipall
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "DiziPall çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # HDFilmsite
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/hdfilmsitedizi.git private_hdfilmsitedizi
          rsync -a yereldiziler/ private_hdfilmsitedizi/
          cd private_hdfilmsitedizi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "HDFilmsite çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # HDFilmCehennemi
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/hdfilmcehennemi.git private_hdfilmcehennemi
          rsync -a hdfilmcehennemi/ private_hdfilmcehennemi/
          cd private_hdfilmcehennemi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "HDFilmCehennemi çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # YabancıDiziler
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/yabancidiziler.git private_yabancidiziler
          rsync -a yabancidiziler/ private_yabancidiziler/
          cd private_yabancidiziler
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "YabanciDiziler çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # SezonlukDİzi
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/sezonlukdizi.git private_sezonlukdizi
          rsync -a sdizi/ private_sezonlukdizi/
          cd private_sezonlukdizi
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "SezonlukDizi çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # Dizipal
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/dizipal.git private_dizipal
          rsync -a dizipal/ private_dizipal/
          cd private_dizipal
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "Dizipal çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # AnimeHeaven
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/animeheaven.git private_animeheaven
          rsync -a animeheaven/ private_animeheaven/
          cd private_animeheaven
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "AnimeHeaven çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..

          # Dizilife & Tlc & Dmax
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git private_m3u8filee
          rsync -a dizilife/ private_m3u8filee/
          cd private_m3u8filee
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add . || echo "No files"
          git commit -m "Dizilife & Tlc & Dmax çıktı güncellendi [auto]" || echo "No changes"
          git push origin main
          cd ..
          
  part4:
    name: Step 15 Çalıştır
    runs-on: ubuntu-latest
    needs: part3
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      # 15. YeniJSON to AnaJSON
      - name: Run yenibolumdenanajsona Script
        run: python private_py/series/yenibolumdenanajsona.py

      - name: Commit and Push Ana JSONs to public repo
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add dizigom/*.json sdizi/*.json anizm/*.json animenosub/*.json setfilmizledizi/*.json diziyo/*.json yabancidiziler/*.json || echo "No files"
          git commit -m "All Ana JSON güncellendi [auto]" || echo "No changes"
          git push origin main

  part5:
    name: Step 16-to-19 Çalıştır
    runs-on: ubuntu-latest
    needs: part4
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

    steps:
      - name: Checkout public repo (main)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Clone Private Py Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/Py.git private_py

      - name: Install Shared Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml aiohttp aiofiles dotenv playwright
          playwright install
       
      # 16. Dizizgom Filmler
      - name: Run dizigomfilmler Script
        run: python private_py/movies/dizigom/dizigomfilmler.py

      # 17. SetFilmİzle Filmler
      - name: Run Setfilm Script
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: python private_py/movies/setfilmizle/ilksayfatümm3uolusturma.py

      # 18. HDFilm Yabancı ve Yerli Filmler
      - name: Run HDFilm Yabancı ve Yerli Filmler Script
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: python private_py/movies/hdfilmsite/hemyabancihemdeyerli.py

      - name: Sync Filmler outputs to private m3u8file repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/zerodayip/m3u8file.git privatate_m3u8file
          cp dizigomfilmler.m3u privatate_m3u8file/dizigomfilmler.m3u || echo "dizigomfilmler.m3u bulunamadı"
          cp xtream/setfilmizlefilmler.m3u privatate_m3u8file/setfilmizlefilmler.m3u || echo "setfilmizlefilmler.m3u bulunamadı"
          cp hdfilmsite.m3u privatate_m3u8file/hdfilmsite.m3u || echo "hdfilmsite.m3u bulunamadı"
          cd privatate_m3u8file
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add setfilmizlefilmler.m3u dizigomfilmler.m3u hdfilmsite.m3u dizipalyerlifilm.m3u
          git commit -m "Filmlerin çıktısı güncellendi [auto]" || echo "No changes"
          git push origin main

      - name: Telegram Success Notification
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        if: success()
        run: |
          MESSAGE="✅ M3U dosyaları başarıyla güncellendi!"
          curl -s -o /dev/null -X POST "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE"
